[package]
name = "websockets-chat-threads"
version = "0.1.0"
edition = "2021"

[dependencies]
websockets = { path = "../websockets" }
bytes = "1.7.2"
uuid = { version = "1.10.0", features = ["v4", "fast-rng", "macro-diagnostics"] }

